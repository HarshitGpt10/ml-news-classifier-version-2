# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Deep Learning (CPU-only — no torchvision/torchaudio)
torch>=2.3.0,<2.5.0

# HuggingFace (transformers bundles tokenizers; accelerate/datasets not needed at runtime)
transformers>=4.41.0

# Core ML (baseline TF-IDF+LR model loaded via joblib)
scikit-learn>=1.5.0
joblib>=1.4.0
numpy>=1.26.0,<2.1.0

# Text preprocessing
nltk>=3.8.0

# OCR (easyocr bundles its own OpenCV — no opencv-python-headless needed)
easyocr>=1.7.0
pytesseract>=0.3.10
Pillow>=10.3.0

# Translation
deep-translator>=1.11.0
langdetect>=1.0.9

# Model download from Google Drive
gdown>=5.1.0

